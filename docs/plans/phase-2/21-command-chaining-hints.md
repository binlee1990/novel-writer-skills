# Task 21: 命令间自动衔接提示

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在每个命令执行完成后，自动推荐下一步操作，形成流畅的创作工作流

**Architecture:** 在各命令的输出尾部（后置处理或结束部分）统一添加「下一步建议」模块。建议内容基于当前命令的执行结果和故事状态动态生成。

**Tech Stack:** Markdown 模板

---

### Task 21.1: 定义统一的衔接提示模板

**Step 1: 定义通用模板格式**

所有命令的衔接提示使用统一格式：

```markdown
━━━━━━━━━━━━━━━━━━━━
💡 **下一步建议**：
1️⃣ [最推荐的操作] — [简短理由]
2️⃣ [次推荐的操作] — [简短理由]
3️⃣ [可选操作] — [简短理由]
```

---

### Task 21.2: 为各命令添加衔接提示

**Files:**
- Modify: `templates/commands/specify.md`（输出尾部）
- Modify: `templates/commands/clarify.md`（输出尾部）
- Modify: `templates/commands/plan.md`（输出尾部）
- Modify: `templates/commands/tasks.md`（输出尾部）
- Modify: `templates/commands/write.md`（后置处理尾部）
- Modify: `templates/commands/analyze.md`（报告尾部）
- Modify: `templates/commands/recap.md`（简报尾部）
- Modify: `templates/commands/track.md`（输出尾部）
- Modify: `templates/commands/character.md`（输出尾部）

**Step 1: 各命令的衔接提示内容**

##### /specify 完成后
```
💡 下一步建议：
1️⃣ /clarify [故事名] — 澄清规格书中的待定项（如有 ⚠️ 标记）
2️⃣ /plan [故事名] — 制定创作计划（如规格已完整）
3️⃣ /specify --world — 补充世界观详细设定（如需要）
```

##### /clarify 完成后
```
💡 下一步建议：
1️⃣ /plan [故事名] — 制定创作计划（推荐）
2️⃣ /specify [故事名] — 继续完善规格书（如仍有待定项）
3️⃣ /character create — 创建角色档案
```

##### /plan 完成后
```
💡 下一步建议：
1️⃣ /tasks [故事名] — 生成写作任务列表（推荐）
2️⃣ /plan --detail vol-01 — 细化第一卷逐章规划
3️⃣ /character create — 创建/补充角色档案
```

##### /tasks 完成后
```
💡 下一步建议：
1️⃣ /write [第一个任务的章节号] — 开始写作（推荐）
2️⃣ /recap [故事名] — 查看写作前上下文简报
3️⃣ /character list — 确认角色状态
```

##### /write 完成后
```
💡 下一步建议：
1️⃣ /write [下一章节号] — 继续写作下一章（推荐）
2️⃣ /analyze [故事名] --range=ch[N] — 分析刚写完的章节
3️⃣ /recap [故事名] — 查看最新上下文简报
[如果写完一卷]
4️⃣ /analyze [故事名] --range=vol-[X] — 分析本卷质量
```

##### /analyze 完成后
```
💡 下一步建议：
[根据分析结果动态生成]
- 如有严重问题 → 1️⃣ 修复 [具体问题] — [具体建议]
- 如无严重问题 → 1️⃣ /write [下一章] — 继续写作
- 通用 → 2️⃣ /track --check — 检查 tracking 数据健康度
```

##### /recap 完成后
```
💡 下一步建议：
1️⃣ /write [下一章节号] — 开始/继续写作（推荐）
2️⃣ /write --fast [下一章节号] — 快写模式（灵感充足时）
3️⃣ /character list — 查看角色状态
```

##### /track 完成后
```
💡 下一步建议：
[根据 --check 结果动态生成]
- 如有警告 → 1️⃣ 处理 [具体警告]
- 如无警告 → 1️⃣ /write [下一章] — 继续写作
- 通用 → 2️⃣ /analyze [故事名] — 质量分析
```

##### /character 完成后
```
💡 下一步建议：
[根据子命令动态生成]
- create 后 → 1️⃣ /character voice [角色名] — 设定对话风格
- list 后 → 1️⃣ /write [下一章] — 继续写作
- voice 后 → 1️⃣ /write [下一章] — 开始写作（对话风格已就绪）
```

**Step 2: 逐个命令添加衔接提示**

在每个命令文件的输出尾部（通常是最后一个 section 之后），添加对应的衔接提示模板。

**注意**：
- 衔接提示应该是命令输出的最后一部分
- 提示内容应根据实际执行结果动态调整（不是固定文本）
- 如果命令已有「建议行动」部分，将衔接提示整合进去而非重复

**Step 3: 分批 Commit**

```bash
git add templates/commands/specify.md templates/commands/clarify.md templates/commands/plan.md
git commit -m "feat: add next-step chaining hints to /specify, /clarify, /plan"

git add templates/commands/tasks.md templates/commands/write.md templates/commands/analyze.md
git commit -m "feat: add next-step chaining hints to /tasks, /write, /analyze"

git add templates/commands/recap.md templates/commands/track.md templates/commands/character.md
git commit -m "feat: add next-step chaining hints to /recap, /track, /character"
```
